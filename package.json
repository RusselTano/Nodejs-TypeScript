{
  "name": "node-starter",
  "private": true,
  "scripts": {
    "start": "concurrently \"tsc -w\" \"nodemon public/js/server.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@types/express": "^5.0.0",
    "concurrently": "^9.0.0",
    "express": "^4.21.2",
    "nodemon": "^3.1.9",
    "typescript": "^5.7.2"
  }
}
